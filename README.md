# medium-golang-github-webhooks

ngrok http localhost:3000
ngrok_forwarding
    You can use the "https" one

gh_repo_settings_webhooks

gh_add_webhook (payload url, json, secret), pay attention to path
gh_issue_comments

golang_parse_error_ping_event -> we don't check for the ping event, but return '200' anyway
gh_webhook_success_ping_event 

gh_create_issue -> Don't trigger the event

gh_write_comment

webhook_success